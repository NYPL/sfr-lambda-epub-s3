language: node_js
node_js:
- '6.10'
install: npm install
script: npm test
cache:
  directories:
  - node_modules
after_success:
- ENVIRONMENT_NAME=$TRAVIS_BRANCH
- if [ "$TRAVIS_BRANCH" == "master" ]; then ENVIRONMENT_NAME=production; fi
